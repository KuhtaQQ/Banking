version: '3.6'

services:
  rmt-new-info:
    image: $AND_DOCKER_REGISTRY_NAME/$PROJECT-$APP:$CI_COMMIT_BRANCH
    container_name: $APP-$CI_COMMIT_BRANCH
    env_file: $AND_ENV
    ports:
      - 9005:8080
    restart: always
